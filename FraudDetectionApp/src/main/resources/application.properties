spring.application.name=FraudDetectionApp
spring.datasource.url = jdbc:postgresql://localhost:5432/transactions
spring.datasource.username = postgres
spring.datasource.password = abd123
spring.datasource.driver-class-name = org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
#we use update (if the table is not exsist then create it else update it)
#create (it will create and drop table each time run the app)
spring.jpa.show-sql = true

server.port = 8081
